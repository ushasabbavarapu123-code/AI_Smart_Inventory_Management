# Project root environment variables template
# For the Express backend configuration, please configure app/.env using these fields:

PORT=5000
NODE_ENV=development
DB_PATH=../data/inventory.db
JWT_SECRET=supersecretkey
